node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.env.production
.idea
.vscode
*.log
.vercel
.env*.local

# Base de prospeccao com dados pessoais — NUNCA commitar (repo publico)
crm_base_omniflow.json

# Tabela de preco do Fast Delivery: dado comercial (margens por destino).
# Fica so na maquina de quem carrega, nunca no repositorio.
*.csv
